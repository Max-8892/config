  sudo -s
# 更新apt-get
  apt-get update
# 安装python包管理工具
  apt-get install python-setuptools
  apt-get install python-pip
# 安装hadowsocks
  pip install shadowsocks
  创建配置文件

mkdir /etc/shadowsocks

vim /etc/shadowsocks/ss.json

配置文件内容：

这里先按insert键，再输入内容
{   
"server":"0.0.0.0",   
"server_port":443,
"local_address":"127.0.0.1",   
"local_port":1080,   
"password":"Asr-9000",
"timeout":600,  
"method":"rc4-md5",
"fast_open":false,
"workers": 1
}
启动：ssserver -c /etc/shadowsocks/ss.json -d start

停止：ssserver -c /etc/shadowsocks/ss.json -d stop

重启：ssserver -c /etc/shadowsocks/ss.json -d restart

vi /etc/rc.local
sudo ssserver -c /etc/shadowsocks/ss.json -d start
outline官方地址：https://getoutline.org/
服务器上使用的代码：

1： sudo -i

2： curl -sS https://get.docker.com/ | sh

3：systemctl start docker

4：systemctl enable docker

5：systemctl status docker

6：wget -qO- https://raw.githubusercontent.com/Jigsaw-Code/outline-server/master/src/server_manager/install_scripts/install_server.sh | bash

测试脚本：
curl -fsSL https://ilemonrain.com/download/shell/LemonBench.sh | bash -s fast

卸载：
sudo apt-get --purge remove <软件名>


cp /mnt/c/HK.pem ~/WSL-path/HK.pem

ssh -i /root/HK.pem ubuntu@ec2-18-163-101-60.ap-east-1.compute.amazonaws.com



6VHl+aaPa{a2c*_
@})>JXWY%qlB%Gw

https://github.com/Max-8892/config/blob/main/default.mobileconfig